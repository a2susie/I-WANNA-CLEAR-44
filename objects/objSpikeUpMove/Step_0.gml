/// @description Insert description here
// You can write your code in this editor
if (atime=931)
{
if point_distance(x, y, objPointer.x, objPointer.y) > 5
{
    move_towards_point(target.x, target.y, 5);
}
else speed = 0;
}